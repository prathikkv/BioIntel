# Ultra-Minimal Requirements for Vercel (Crash-Proof)
# Only the absolute essentials to prevent function crashes

# Core FastAPI - Minimal version
fastapi==0.104.1
uvicorn==0.24.0

# Basic validation  
pydantic==2.5.0

# Configuration (minimal)
python-dotenv==1.0.0